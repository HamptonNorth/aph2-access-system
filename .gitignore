node_modules/
*.log

# SQLite database files (generated by db:init)
db/*.sqlite
db/*.sqlite-journal
db/*.sqlite-wal
db/*.sqlite-shm

# Member-data CSVs and similar imports - real names / fob numbers should
# never be committed. Drop import sources in db/ but keep them out of git.
db/*.csv
db/*.tsv

# Editor / OS noise
.DS_Store
.vscode/
.idea/

# curl_tests cookie jar (per-run session state)
curl_tests/cookies.txt

# Client bundle output (regenerated by `bun run client:build`)
client/dist/
